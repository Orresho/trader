import React from "react";

const ItemListComponent = ({ itemType, name, description }) => {
    return (
        <div>
            <h3>{console.log(itemType)}</h3>
        </div>
    )
}

export default ItemListComponent;